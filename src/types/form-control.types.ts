export interface FormControlProps {
  label?: string;
  description?: string;
  placeholder?: string;
  classNames?: {
    container?: string;
    label?: string;
    input?: string;
    description?: string;
  };
}
